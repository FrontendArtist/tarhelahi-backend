'use strict';

/**
 * top-banner controller
 * Strapi v5 — Single Type
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::top-banner.top-banner');
