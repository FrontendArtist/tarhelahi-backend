'use strict';

/**
 * bank-setting router
 * Strapi v5 — Single Type
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::bank-setting.bank-setting');
