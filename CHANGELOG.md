# CHANGELOG

## [2026-09-24]

- بهینه‌سازی جامع درخواست‌های کلاینت و حذف کامل پولینگ‌های پس‌زمینه در فرانت‌اند (Zero Idle Background Requests):
  - حذف کامل پولینگ ۵ ثانیه‌ای `setInterval` از مدال جزئیات پیام‌ها (`MessageDetailModal.jsx`) و جلوگیری از فچ مکرر کل پیام‌های کاربر با `getMyMessages` (کاهش ۲۴ درخواست سنگین در دقیقه به ازای هر کاربر).
  - حذف کامل پولینگ ۵ ثانیه‌ای از پنل ارتباط با استاد (`InstructorChatPanel.jsx`) و اضافه کردن دکمه بروزرسانی دستی زیبا در سایدبار پنل استاد.
  - حذف تایمر دوره‌ای ۴۵ ثانیه‌ای از هوک سراسری اعلان‌ها (`useOrderNotifications.js`) جهت توقف ۱۰۰٪ ارسال درخواست در حین حضور و سکون کاربر در صفحات (نظیر پخش صوت دوره‌ها) و محدودسازی فچ به لود اولیه و تغییر تب با تراتل ۶۰ ثانیه‌ای.
  - بهینه‌سازی ترکر بازدیدکنندگان (`VisitorTracker.jsx`): افزایش فاصله ارسال Heartbeat از ۶۰ ثانیه به ۱۲۰ ثانیه و اعمال تراتل ۶۰ ثانیه‌ای هماهنگ بر روی رویداد `visibilitychange` بدون اثر منفی بر دقت آمار آنلاین‌ها و بازدید روزانه.
- یکپارچه‌سازی دریافت سفارشات و رفع معضل فچ‌های موازی در Lazy Loading (Single Source of Fetch):
  - افزودن قابلیت In-Flight Promise Deduplication به `useOrdersStore.js` و `useUserMessagesStore.js` جهت اتصال درخواست‌های موازی به یک تک‌پرامیس و ارسال فقط یک درخواست HTTP به شبکه.
  - تبدیل کارت‌های دوره (`CourseCard.jsx`)، دکمه‌های خرید (`AddToCartButton.jsx` و `ProductAddToCart.jsx`) و تب‌های دوره (`CourseTabs.jsx` و `CourseContentManager.jsx`) به مصرف‌کننده صرف (Pure Consumer) و حذف فراخوانی `fetchOrders()` از `useEffect` آن‌ها؛ این تغییر باعث شد هنگام اسکرول و Lazy Load در کاتالوگ دوره‌ها هیچ درخواستی به شبکه ارسال نشود و وضعیت مالکیت دوره‌ها آنی از حافظه رم ارزیابی گردد.
  - مشروط‌سازی واکشی سفارشات در `OrdersList.jsx` و `PurchasesList.jsx` به شرط عدم واکشی قبلی (`!hasFetched`).
- بهینه‌سازی دریافت اطلاعات پروفایل و کش سروری:
  - اعمال In-Flight Promise Deduplication و کش درون‌برنامه‌ای ۱ دقیقه‌ای در `profileClientApi.js` جهت یکی کردن فچ‌های موازی `/api/profile` در زمان بارگذاری همزمان نوبار (`UserStatus.jsx`) و همگام‌ساز سبد خرید (`useCartSync.js`).
  - ارتقای زمان کش Revalidation شبکه‌های اجتماعی در `SocialMedia.jsx` از ۶۰ ثانیه به ۲۴ ساعت (`86400`).

## [2026-09-22]

- پیاده‌سازی بخش جامع و نوین «کلام نور» (قرآن کریم و تفاسیر اختصاصی) در بک‌اند و فرانت‌اند:
  - **بک‌اند (Strapi 5)**:
    - ایجاد Content-Type جدید `surah` با فیلدهای شماره سوره (`surahNumber`)، عنوان (`title`)، سوئیچ نمایش همه آیات (`showAllVerses`) و کامپوننت تکرارشونده `ayeha`.
    - ایجاد کامپوننت اختصاصی `quran.ayah-tafsir` شامل فیلدهای شماره آیه (`verseNumber`)، متن عربی (`ayeText`)، صوت عربی اختصاصی (`ayeAudio`)، متن ترجمه فارسی (`translationText`)، صوت ترجمه فارسی اختصاصی (`translationAudio`)، متن تفسیر اختصاصی (`tafsirText`) و فایل صوت تفسیر (`tafsirAudio`) جهت ایجاد قابلیت بارگذاری مستقیم فایل‌ها و متون در بک‌اند و عدم وابستگی به APIهای خارجی.
    - حذف مدل زائد و موقت `tafsir` و یکپارچه‌سازی کامل ساختار داده در Content-Type سوره.
  - **فرانت‌اند (Next.js)**:
    - پیاده‌سازی معماری اولویت دیتای محلی (Local Backend Priority with External API Fallback): اولویت با متن و صوت ثبت‌شده در استراپی و استفاده از API خارجی تنها به عنوان پشتیبان در صورت خالی بودن فیلدها.
    - پشتیبانی کامل از شرط `showAllVerses`: نمایش تمام آیات سوره در صورت فعال بودن، و نمایش منحصراً آیات ثبت‌شده در صورت غیرفعال بودن.
    - افزودن لایه ارتباطی امن و بهینه‌سازی کوئری‌های Strapi v5 با وضعیت `draft` جهت نمایش لحظه‌ای تغییرات ذخیره‌شده.
    - اضافه شدن لینک «کلام نور» به منوی نوبار سایت در هر دو حالت دسکتاپ و موبایل.
    - استانداردسازی ساختار layout صفحه بر اساس ساختار یکپارچه سایت (`<main className={styles.main}>` و کانتینر مرکزی).

## [2026-09-16]

- رفع ریکوئست‌های مکرر و بی‌دلیل در صفحه `/profile/purchases` و `/profile/orders`: حذف پارامتر `force=true` از تابع `fetchOrders` در `PurchasesList.jsx` و `OrdersList.jsx` و اصلاح آرایه وابستگی‌های `useEffect` برای جلوگیری از دور زدن گارد کش استور زوستاند.
- بازطراحی سیستم پولینگ اعلان‌ها در هوک `useOrderNotifications.js` به الگوی **Singleton** در سطح ماژول: جلوگیری از ایجاد چندین تایمر موازی هنگام استفاده همزمان کامپوننت‌های `NotificationBell`، `OrderConfirmedBanner` و `MessagesList` از یک هوک؛ حالا در کل اپلیکیشن فقط یک `setInterval` ۴۵ ثانیه‌ای فعال است.
- افزودن Throttle ۳۰ ثانیه‌ای برای رویداد `visibilitychange` در هوک اعلان‌ها: جلوگیری از ارسال رگبار ریکوئست هنگام سوئیچ سریع بین تب‌های مرورگر یا باز/بسته کردن DevTools.
- تبدیل خواندن کل استور Zustand (`useOrdersStore()` / `useUserMessagesStore()`) به سلکتورهای اختصاصی در کامپوننت‌های `CourseCard.jsx`، `AddToCartButton.jsx`، `ProductAddToCart.jsx` و صفحه پیام‌ها: کاهش ری‌رندرهای غیرضروری ناشی از تغییرات state نامرتبط.
- اصلاح آرایه وابستگی‌های `useEffect` در `CourseTabs.jsx`، `CourseContentManager.jsx` و صفحه `messages/page.jsx` برای حذف توابع استور از deps و جلوگیری از ریکوئست‌های loop-مانند.
- جلوگیری از واکشی تکراری کلاینت‌سایدِ محصولات در `ProductGrid.jsx` پس از بارگذاری SSR: افزودن guard `isInitialMount` و همگام‌سازی state داخلی با داده‌های سرور از طریق یک `useEffect` جداگانه؛ این مشکل باعث فلیکر اسکلتون در لود اولیه صفحه محصولات می‌شد.

## [2026-09-10]

- افزودن کنترلر و روت اختصاصی تسویه دسته‌ای (`POST /api/orders/bulk-settle`) جهت تسویه آنی صدها سفارش واجد شرایط با یک تراکنش مستقیم دیتابیس (batchInsert در جدول پیوند `orders_settlement_lnk`) و جلوگیری از تایم‌اوت در پردازش‌های سنگین مالی.
- پیاده‌سازی کامل سیستم کدهای تخفیف (`coupon`) با اعتبارسنجی هوشمند تاریخ انقضا، سقف مصرف، شمول همگانی یا اقلام خاص، و پشتیبانی خودکار از فصل‌های دوره‌های آموزشی (`isChaptered`).

## [2026-09-09]

- اصلاح متد `find` در کنترلر سفارشات (`src/api/order/controllers/order.js`) برای رفع خطای PaginationError در Strapi 5 هنگام مرتب‌سازی بر اساس اولویت وضعیت (`statusPriority=true`).
- جلوگیری از تداخل انواع صفحه‌بندی (آفست و صفحه‌ای) و اعمال سقف مناسب جهت مرتب‌سازی صحیح تمام سفارش‌های مطابق با جستجو و فیلترها.
- فعال‌سازی دسترسی کامل توکن API سیستم برای Content-Type جدید `settlement` در دیتابیس جهت جلوگیری از خطای ۴۰۳ و اعتبارسنجی روابط در کوئری‌ها.
- رفع خطای ۵۰۰ (numeric field overflow) در جدول `settlements` دیتابیس PostgreSQL با تغییر نوع فیلد `totalAmount` از `decimal` به `biginteger` و افزودن مایگریشن خودکار (`database/migrations/2026.09.09T00.00.00.settlements-total-amount.js`) برای پشتیبانی از ارقام مالی بالای ۱۰۰ میلیون تومان/ریال.

## [2026-09-08]

- ایجاد Content-Type جدید `settlement` (تسویه حساب مالی) در استراپی شامل فیلدهای عنوان (`title`)، تاریخ تسویه (`settlementDate`)، درآمد کل (`totalRevenue`)، تعداد سفارشات (`ordersCount`)، توضیحات (`notes`)، وضعیت (`status`) و رابطه یک‌به‌چند با سفارشات (`orders`).
- به‌روزرسانی اسکیمای `order` و افزودن رابطه `settlement` (چند به یک) به Content-Type سفارش برای اتصال به دوره‌های مالی بدون ایجاد اثر جانبی (Side-effect) بر دسترسی کاربران به دوره‌ها.
- به‌روزرسانی تایپ‌های تولیدشده در `types/generated/contentTypes.d.ts`.

## [2026-09-05]

- اصلاح کامپوننت `VideoJSPlayer.jsx` برای حذف استفاده از `MediaWatermark` و پاکسازی کدهای تکراری.
- افزودن محافظت‌های پیشرفته ضد دانلود (controlsList="nodownload" و مسدودسازی کلیک راست و کلیدهای میانبر) در پلیر ویدیو.
- بهبود ذخیره‌سازی پیشرفت پخش (Resume Playback) با به‌روزرسانی‌های دقیق‌تری برای زمان ذخیره‌سازی هر 5 ثانیه.
- به‌روزرسانی آیکون‌های سفارشی ۱۰ ثانیه جلو/عقب.
- بهینه‌سازی چرخه حیات پلیر برای جلوگیری از حافظه‌لکه.

این تغییرات به منظور افزایش امنیت محتوا و جلوگیری از دانلود و ضبط صفحه برای دوره‌های آموزشی اعمال شد.
