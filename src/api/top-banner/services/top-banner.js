'use strict';

/**
 * top-banner service
 * Strapi v5 — Single Type
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::top-banner.top-banner');
