'use strict';

/**
 * bank-setting service
 * Strapi v5 — Single Type
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bank-setting.bank-setting');
