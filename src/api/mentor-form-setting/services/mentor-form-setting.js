'use strict';

/**
 * mentor-form-setting service
 * Strapi v5 — Single Type
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mentor-form-setting.mentor-form-setting');
