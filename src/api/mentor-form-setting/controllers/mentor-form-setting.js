'use strict';

/**
 * mentor-form-setting controller
 * Strapi v5 — Single Type
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mentor-form-setting.mentor-form-setting');
